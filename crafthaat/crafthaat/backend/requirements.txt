# ---------------------------------------------------------------------------
# CraftHaat - Backend dependencies
# Install with:
#   pip install -r requirements.txt
#
# NOTE: rembg / faster-whisper / sentence-transformers pull in torch.
# For a CUDA box, install the matching torch wheel FIRST, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# ---------------------------------------------------------------------------

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
pgvector==0.3.4
alembic==1.13.2

# Object storage
minio==7.2.9

# AI pipeline
rembg==2.0.59
onnxruntime==1.19.2          # CPU build. Swap for onnxruntime-gpu if CUDA is available.
faster-whisper==1.0.3
sentence-transformers==3.1.1
httpx==0.27.2
Pillow==10.4.0
numpy==1.26.4

python-dotenv==1.0.1
